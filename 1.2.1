#include <iostream>
using namespace std;

int main()
{
    bool flag;
    int n, j;
    cin >> n;
    for (int i=2; i<n; i++) {
        flag=true;
        for (j=2; j<i; j++)
            if (!(i%j)) {
                flag=false;
                break;
            };
        if (flag)
            cout<<j<<endl;
    }

    return 0;
}
